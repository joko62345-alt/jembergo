<!doctype html>
<html lang="id">
<head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>{{ $filter === 'active' ? 'Tiket Saya' : ($filter === 'history' ? 'Riwayat Tiket' : 'Pesanan Saya') }} | JemberGo</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"><link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet"><link href="{{ asset('css/jembergo-fallback.css') }}" rel="stylesheet"></head>
<body class="customer-page"><x-public-navbar /><main class="container orders-page"><div class="orders-header"><div><a href="{{ route('home') }}" class="orders-back"><i class="bi bi-arrow-left"></i> Kembali ke JemberGo</a><span class="orders-eyebrow">Area pelanggan</span><h1>{{ $filter === 'active' ? 'Tiket saya' : ($filter === 'history' ? 'Riwayat tiket' : 'Pesanan saya') }}</h1><p>Lihat status pesanan dan akses e-ticket perjalananmu.</p></div><a href="{{ route('destinations.index') }}" class="orders-new-button"><i class="bi bi-plus-lg"></i> Pesan tiket baru</a></div>
<div class="orders-tabs"><a class="{{ $filter === 'active' ? 'active' : '' }}" href="{{ route('customer.tickets') }}"><i class="bi bi-ticket-perforated"></i> Tiket aktif</a><a class="{{ $filter === 'history' ? 'active' : '' }}" href="{{ route('customer.ticket-history') }}"><i class="bi bi-clock-history"></i> Riwayat tiket</a><a class="{{ $filter === 'all' ? 'active' : '' }}" href="{{ route('customer.orders') }}"><i class="bi bi-grid"></i> Semua pesanan</a></div>
<div class="orders-table-card"><div class="orders-table-heading"><div><strong>Daftar perjalanan</strong><small>{{ $orders->total() }} pesanan tercatat</small></div><i class="bi bi-list-check"></i></div><div class="table-responsive"><table class="table orders-table align-middle mb-0"><thead><tr><th>Kode booking</th><th>Destinasi</th><th>Kunjungan</th><th>Status</th><th class="text-end">Aksi</th></tr></thead><tbody>@forelse($orders as $order)@php($isUsed = $order->tiket->contains('status_tiket', 'USED'))@php($isExpired = $order->tiket->contains('status_tiket', 'EXPIRED'))@php($isCashBooking = $order->pembayaran?->metode_pembayaran === 'CASH')@php($orderStatus = $isUsed ? \App\Support\StatusLabel::ticket('USED') : ($isExpired ? \App\Support\StatusLabel::ticket('EXPIRED') : ($filter === 'history' ? \App\Support\StatusLabel::ticket($order->tiket->first()?->status_tiket) : \App\Support\StatusLabel::order($order->status_pemesanan))))<tr><td><strong class="order-code">{{ $order->kode_booking }}</strong><small class="d-block order-type">{{ $isCashBooking ? 'Tunai di tempat' : 'Payment Gateway' }}</small></td><td><strong>{{ $order->destinasi->nama_wisata }}</strong></td><td><span class="order-date"><i class="bi bi-calendar3"></i>{{ $order->tanggal_kunjungan->translatedFormat('d F Y') }}</span></td><td><span class="order-status {{ $isUsed ? 'success' : ($isExpired ? 'danger' : ($order->status_pemesanan === 'PAID' ? 'success' : 'pending')) }}"><span></span>{{ $orderStatus }}</span></td><td class="text-end">@if($order->status_pemesanan === 'PAID' || ($isCashBooking && $order->tiket->isNotEmpty()))<a href="{{ route('customer.ticket', $order->id_pemesanan) }}" class="order-action outline"><i class="bi bi-ticket-perforated"></i> E-ticket</a>@elseif($order->status_pemesanan === 'PENDING' && ! $isExpired)<a href="{{ route('customer.checkout', $order->id_pemesanan) }}" class="order-action primary">Bayar <i class="bi bi-arrow-right"></i></a>@endif</td></tr>@empty<tr><td colspan="5" class="text-center text-secondary py-5">Belum ada tiket pada bagian ini.</td></tr>@endforelse</tbody></table></div></div><div class="orders-pagination">{{ $orders->links() }}</div></main><x-public-footer /></body></html>