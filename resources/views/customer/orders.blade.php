<!doctype html>
<html lang="id">
<head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>{{ $filter === 'active' ? 'Tiket Saya' : ($filter === 'history' ? 'Riwayat Tiket' : 'Pesanan Saya') }} | JemberGo</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"><link href="{{ asset('css/jembergo-fallback.css') }}" rel="stylesheet"></head>
<body class="bg-light"><main class="container py-5"><div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><a href="{{ route('home') }}" class="text-dark text-decoration-none">Kembali ke JemberGo</a><h1 class="h2 fw-bold mt-2">{{ $filter === 'active' ? 'Tiket saya' : ($filter === 'history' ? 'Riwayat tiket' : 'Pesanan saya') }}</h1></div><a href="{{ route('destinations.index') }}" class="btn btn-warning rounded-pill">Pesan tiket baru</a></div>
<div class="nav nav-pills gap-2 mb-4"><a class="nav-link rounded-pill {{ $filter === 'active' ? 'active' : '' }}" href="{{ route('customer.tickets') }}">Tiket aktif</a><a class="nav-link rounded-pill {{ $filter === 'history' ? 'active' : '' }}" href="{{ route('customer.ticket-history') }}">Riwayat tiket</a><a class="nav-link rounded-pill {{ $filter === 'all' ? 'active' : '' }}" href="{{ route('customer.orders') }}">Semua pesanan</a></div>
<div class="card border-0 shadow-sm rounded-4"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Kode booking</th><th>Destinasi</th><th>Kunjungan</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($orders as $order)<tr><td class="fw-semibold">{{ $order->kode_booking }}</td><td>{{ $order->destinasi->nama_wisata }}</td><td>{{ $order->tanggal_kunjungan->translatedFormat('d F Y') }}</td><td><span class="badge {{ in_array($order->status_pemesanan, ['CANCELLED', 'EXPIRED']) ? 'text-bg-danger' : ($order->status_pemesanan === 'PAID' ? 'text-bg-success' : 'text-bg-warning') }}">{{ $order->status_pemesanan }}</span></td><td>@if($order->status_pemesanan === 'PAID')<a href="{{ route('customer.ticket', $order->id_pemesanan) }}" class="btn btn-sm btn-outline-dark rounded-pill">E-ticket</a>@elseif($order->status_pemesanan === 'PENDING')<a href="{{ route('customer.checkout', $order->id_pemesanan) }}" class="btn btn-sm btn-warning rounded-pill">Bayar</a>@endif</td></tr>@empty<tr><td colspan="5" class="text-center text-secondary py-5">Belum ada tiket pada bagian ini.</td></tr>@endforelse</tbody></table></div></div><div class="mt-4">{{ $orders->links() }}</div></main></body></html>