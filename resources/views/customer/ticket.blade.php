<!doctype html>
<html lang="id">
<head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>E-Ticket | JemberGo</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"><link href="{{ asset('css/jembergo-fallback.css') }}" rel="stylesheet"></head>
<body class="bg-light"><main class="container py-5"><div class="row justify-content-center"><div class="col-lg-9"><a href="{{ route('home') }}" class="text-dark text-decoration-none">Kembali ke JemberGo</a>@if(session('success'))<div class="alert alert-success mt-3">{{ session('success') }}</div>@endif<div class="card border-0 shadow-sm rounded-4 mt-4"><div class="card-body p-4 p-lg-5"><div class="d-flex justify-content-between"><div><span class="text-warning text-uppercase small fw-bold">E-ticket JemberGo</span><h1 class="h2 fw-bold mt-2">{{ $booking->destinasi->nama_wisata }}</h1><p class="text-secondary mb-0">{{ $booking->kode_booking }}</p></div><span class="badge text-bg-success align-self-start">{{ $booking->status_pemesanan }}</span></div><div class="row g-3 border-top border-bottom py-4 my-4"><div class="col-sm-4"><small class="text-secondary d-block">Ketua kelompok</small><strong>{{ $booking->ketua_nama }}</strong></div><div class="col-sm-4"><small class="text-secondary d-block">Kontak</small><strong>{{ $booking->ketua_no_hp }}</strong></div><div class="col-sm-4"><small class="text-secondary d-block">Tanggal kunjungan</small><strong>{{ $booking->tanggal_kunjungan->translatedFormat('d F Y') }}</strong></div></div><div class="alert alert-warning"><strong>Kebijakan pembayaran:</strong> tiket yang sudah dibayar tidak dapat dibatalkan dan tidak dapat di-refund.</div><div class="bg-light rounded-3 p-3 mb-4"><h2 class="h6 fw-bold">Peserta kelompok ({{ 1 + count($booking->anggota_names ?? []) }} orang)</h2><ol class="mb-0"><li>{{ $booking->ketua_nama }} <span class="text-secondary">(Ketua)</span></li>@foreach($booking->anggota_names ?? [] as $member)<li>{{ is_array($member) ? $member['nama'] : $member }}</li>@endforeach</ol></div><h2 class="h5 fw-bold">QR Code tiket</h2><p class="small text-secondary">QR Code berisi data terenkripsi tiket dan dapat diverifikasi oleh petugas.</p>@foreach($booking->tiket as $index => $ticket)<div class="d-flex flex-wrap justify-content-between align-items-center gap-3 bg-light rounded-3 p-3 mt-2"><span>Tiket {{ $index + 1 }}<small class="d-block text-secondary">{{ $ticket->status_tiket }}</small></span><img src="https://api.qrserver.com/v1/create-qr-code/?size=140x140&amp;data={{ urlencode($ticket->kode_qr) }}" width="140" height="140" alt="QR Code tiket {{ $index + 1 }}"><code>{{ substr($ticket->kode_qr, 0, 12) }}...</code></div>@endforeach</div></div></div></div></main></body></html>
