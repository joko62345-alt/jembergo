@include('components.superadmin-sidebar')
<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Laporan | Super Admin JemberGo</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"></head><body class="bg-light"><main class="container py-5"><div class="d-flex justify-content-between align-items-end"><div><h1 class="h2 fw-bold mb-0">Laporan transaksi</h1><p class="text-secondary">Status mengikuti verifikasi tiket di destinasi.</p></div><a href="{{ route('superadmin.report.export', request()->query()) }}" class="btn btn-outline-success rounded-pill">Export CSV</a></div><form class="row g-3 my-4"><div class="col-md-4"><label class="form-label" for="id_destinasi">Destinasi</label><select id="id_destinasi" name="id_destinasi" class="form-select"><option value="">Semua destinasi</option>@foreach($destinations as $destination)<option value="{{ $destination->id_destinasi }}" @selected((string) $destinationId === (string) $destination->id_destinasi)>{{ $destination->nama_wisata }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label" for="from">Dari</label><input id="from" type="date" name="from" value="{{ request('from') }}" class="form-control"></div><div class="col-md-3"><label class="form-label" for="to">Sampai</label><input id="to" type="date" name="to" value="{{ request('to') }}" class="form-control"></div><div class="col-md-2 d-flex align-items-end"><button class="btn btn-warning rounded-pill w-100">Terapkan</button></div></form><div class="card border-0 shadow-sm rounded-4"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Kode booking</th><th>Destinasi</th><th>Tanggal pesan</th><th>Status tiket</th><th>Total</th></tr></thead><tbody>@forelse($orders as $order)@php($ticketStatuses = $order->tiket->pluck('status_tiket'))@php($displayStatus = $ticketStatuses->isNotEmpty() && $ticketStatuses->every(fn ($status) => $status === 'USED') ? 'USED' : ($ticketStatuses->contains('USED') ? 'PARTIAL' : $order->status_pemesanan))<tr><td>{{ $order->kode_booking }}</td><td>{{ $order->destinasi->nama_wisata }}</td><td>{{ $order->tanggal_pemesanan->format('d/m/Y H:i') }}</td><td><span class="badge {{ $displayStatus === 'USED' ? 'text-bg-success' : ($displayStatus === 'PARTIAL' ? 'text-bg-warning' : 'text-bg-light') }}">{{ $displayStatus }}</span><small class="d-block text-secondary">Pembayaran: {{ $order->status_pemesanan }}</small></td><td>Rp {{ number_format($order->total_harga, 0, ',', '.') }}</td></tr>@empty<tr><td colspan="5" class="text-center text-secondary py-5">Belum ada transaksi.</td></tr>@endforelse</tbody></table></div></div><div class="mt-4">{{ method_exists($orders, 'links') ? $orders->links() : '' }}</div></main></body></html>